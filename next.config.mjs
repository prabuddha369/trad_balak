const nextConfig = {
    images: {
        domains: ['assets.ajio.com', 'assets.myntassets.com', 'images.bewakoof.com', 'encrypted-tbn0.gstatic.com', 'louisphilippe.abfrl.in', 'i.pinimg.com', 'previews.123rf.com'],
    },
};

export default nextConfig;
